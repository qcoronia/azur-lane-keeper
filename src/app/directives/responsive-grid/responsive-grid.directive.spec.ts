import { ResponsiveGridDirective } from './responsive-grid.directive';

describe('ResponsiveGridDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveGridDirective();
    expect(directive).toBeTruthy();
  });
});
